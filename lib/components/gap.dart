import 'package:flutter/material.dart';

class Gapz extends StatelessWidget {
  const Gapz({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 8);
  }
}