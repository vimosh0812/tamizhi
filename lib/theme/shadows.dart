import 'package:flutter/material.dart';

class AppShadows {
  static const BoxShadow tertiaryShadow = BoxShadow(
    color: Color.fromRGBO(93, 104, 116, 0.01),
    offset: Offset(1, 1),
    blurRadius: 10,
    spreadRadius: 10,
  );
}
